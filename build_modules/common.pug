include ../src/components/switch-button/switch-button
include ../src/components/link/link
include ../src/components/slider-buttons/slider-buttons
include ../src/components/state-button/state-button
include ../src/components/example-button/example-button

mixin svg(id)
    svg(focusable = 'false')&attributes(attributes)
        use(xlink:href=`#${id}`)


<!DOCTYPE html>
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Задание 2

    body
        block body
