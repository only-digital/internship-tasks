mixin confirm(props)
	.confirm&attributes(attributes)(data-component='confirm')
		label&attributes(attributes)(for="confirm").confirm__label
					input&attributes(attributes)(type="checkbox" id="confirm" name="confirm" required).confirm__input
					+svg('check-mark').confirm__svg
					| Вы подтверждаете, что ознакомились c&nbsp;  
					a(href='/#').confirm__link  политикой обработки персональных данных 


//- to make disable styles work: add .disable class to label and :disable atribute to input
//- to realy disable link - add a.addEventListener('click', function(event) {
//-  event.preventDefault();
//- });