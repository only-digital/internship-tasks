mixin slider-button(props)
	.slider-button
		.btn-group
			button&attributes(attributes)
				span.icon
					+svg('button-left')
			button&attributes(attributes)
				span.icon
						+svg('button-right')
		.counter
			span 01 / 
			span= props.number