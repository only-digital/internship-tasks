mixin link(props)
	if props.disabled
		.link-wrapper
			a.link.disabled&attributes(attributes)= props.text 
				+svg('link-icon')(width='20px' height='20px')
	else 
		.link-wrapper&attributes(attributes)
			a.link&attributes(attributes)= props.text 
				+svg('link-icon')(width='20px' height='20px')