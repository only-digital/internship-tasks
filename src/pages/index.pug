extends ../../build_modules/common


block body
    +example-button({text: 'example text'})
    +example-button({text: 'disabled'})(disabled='')
    
    .div(style='margin:20px')
        +only-btn({text:'Кнопка'})(class='only-btn_main-btn')
    .div(style='margin:20px')
        +only-btn({text:'Кнопка'})(class='only-btn_alt-btn')
    .div(style='margin:20px')
        +only-btn({text:'Кнопка'})(class='only-btn_main-btn' disabled)
    .div(style='margin:20px')
        +only-btn({text:'Кнопка'})(class='only-btn_alt-btn' disabled)
    
    .div(style='margin:20px')
        +only-link({text:'https://example.io'})(href='https://example.io')
    .div(style='margin:20px;font-family:Arial;font-size:16px;font-weight:400;')
        |Для подключения сервиса добавьте  номер
        +only-link({text:'+7(921)058-57-19'})(class='only-link_iconless' href='https://example.io')
        |в ваши контакты и напишите любое сообщение, или перейдите по ссылке ниже
        |Цифровые продукты, направленные на создание 
        +only-link({text:'дополнительных сервисов'})(class='only-link_iconless')
        | и устранение «проблемных точек» наших клиентов
    .div(style='margin:20px')
        +only-link({text:'+7(921)058-57-19'})(disabled)

    .div(style='margin:20px')
        +only-switcher()(class='only-switcher_active')
    .div(style='margin:20px')
        +only-switcher()(class='only-switcher_disabled')

    .div(style='margin:20px')
        +only-slider({page_counts:'4'})
    .div(style='margin:20px')
        +only-slider({page_counts:'3'})(disabled)